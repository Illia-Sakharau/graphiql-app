export type LoginType = {
  email: string;
  password: string;
};

export type RegistrationType = {
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
};

export type AddNewApiType = {
  api: string;
};
